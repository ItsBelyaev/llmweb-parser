version: "3.9"

services:
  app:
    build: .
    container_name: llmweb_parser
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/llmweb.db
    restart: unless-stopped

volumes:
  db_data:
    driver: local
